services:
  mein-gemini-bot:
    image: librechat/librechat:latest
    environment:
      - GOOGLE_KEY=dein_google_api_key_hier  # Hier kommt dein Google Key rein
      - PROVIDER=google                      # Du sagst der App: "Nutze Google"
    ports:
      - "8080:80"
